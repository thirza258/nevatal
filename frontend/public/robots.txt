# https://chat.nevatal.tech/robots.txt
# Change the host here, in index.html and src/constant.ts, then regenerate the sitemap.

User-agent: *
Allow: /

# Nothing under /api/ is a page — it is the JSON backend, and every endpoint
# needs an API key session anyway.
Disallow: /api/

# The home page, About and Courses are public and listed in the sitemap.
# Tool routes remain behind an API key and redirect signed-out visitors home.
# They are not disallowed, so crawlers can follow that redirect and read metadata.

Sitemap: https://chat.nevatal.tech/sitemap.xml
